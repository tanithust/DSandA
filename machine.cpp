#include <iostream>
#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base :: sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int start[1000000],end[1000000],res[1000000],n;
    cin >> n;
    for(int i = 0; i < n; i++){
        cin >> start[i] >> end[i];
    }
    res[0] = end[0] - star[0];
    for(int i = 1; i < n; i++){
        
    }
}